import React from "react";
import { Typography } from "@material-tailwind/react";

export default function LaunchpadHeader() {
  return (
    <div className="mt-7">
      <p className="text-kojiki-blue">launchpad</p>
      <span>
        let's change this to custom-built infrastructure for BASE native public
        sales
      </span>
    </div>
  );
}
